/**
 * Single shared source of the site's base URL, used by metadata files
 * (layout, robots, sitemap) so the host string is never repeated.
 */
export const SITE_URL = new URL("https://keepgoing.you");
